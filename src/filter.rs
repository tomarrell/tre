struct Actions {
    print: bool,
    dive: bool,
}

fn validate_path(options: Options) -> bool {
    true
}

#[cfg(tests)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!("Hello", "Hello");
    }
}
